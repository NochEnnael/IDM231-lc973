/*const myVariable = 'hello';
let myResponse;

switch (myVariable) {
    case 'hello':
        myResponse = 'hi';
        break;    
    case 'howdy':
        myResponse = 'howdy to you too';
        break;
    default:
        myResponse = 'I am confused...;'
}
 //need to put break into after each case becuase it ends it and allows fro the next case
 
 console.log(myResponse) 
*/

const scores = [24,32,17]
const scoresArrayLength = scores.length; 